mkdir -p ${HOME}/qr
cp ./*.key  ${HOME}/qr/
cp ./libconfig.key ${HOME}/qr/libconfig.a
cp ./qrio.cfg ${HOME}/qr

cp ./libshm_alloc ${HOME}/qr/libshm_alloc.so
cp ./bind_cpu.config  ${HOME}/qr/
cp ./tools.config  ${HOME}/qr/

